# This first test, we are going to do a test of 6 runs
# we will do it for 2 scenarios (so netest 1 and netest 2) and 3 instances each (so random seeds 1, 2, 3)
# Let's assume the following:
# - each run takes 3 hours
# - each run is 3G for memory
# - let's do 2 processes on a node for EMEWS
# - then let's do 2 nodes
# - so 2 nodes, with 4 PPN, - the 4 ppn means we can do 2 runs in parallel on a machine 
#     (i.e. 2 nodes for swift, and 2 nodes for computing)
# - on 2 nodes, we can do 4 simulations in parallel - 2 on 2 nodes times 2 machines
# - so that means, we will need to have a wall time of 3 hours times 2 as we will need to do this twice
# - so let's do 10 hours  
# - and we will not set the memory right now 
CFG_WALLTIME=10:00:00
CFG_QUEUE="normal"
CFG_PROJECT=p32153
NODES=2
CFG_PPN=4
CFG_PROCS=$(( NODES * CFG_PPN ))
CFG_SITE=quest


# TODO: Update with path to upf file, relative
# to emews project root directory.
# CFG_UPF=data/upf.txt
CFG_UPF=../../testsweep1_upf.txt
# data/upf.txt
